describe("google.js", () => {
  describe("analyseSentiments", () => {
    it("should be a function", () => {
      expect(analyseSentiments).to.be.a("function");
    });
  });

  describe("processResults", () => {
    it("should be a function", () => {
      expect(processResults).to.be.a("function");
    });
  });
});
