describe("helpers.js", () => {
  describe("hideElement", () => {
    it("should be a function", () => {
      expect(hideElement).to.be.a("function");
    });
  });

  describe("showElement", () => {
    it("should be a function", () => {
      expect(showElement).to.be.a("function");
    });
  });

  describe("appendPosts", () => {
    it("should be a function", () => {
      expect(appendPosts).to.be.a("function");
    });
  });
});
