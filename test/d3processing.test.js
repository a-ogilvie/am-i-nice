describe("d3processing.js", () => {
  describe("drawGraph", () => {
    it("should be a function", () => {
      expect(drawGraph).to.be.a("function");
    });
  });

  describe("processResults", () => {
    it("should be a function", () => {
      expect(processResults).to.be.a("function");
    });
  });
});
